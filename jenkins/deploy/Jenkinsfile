pipeline {
    agent {
        label 'amazon-linux'
    }

    stages {
        stage('CDK Deploy'){
            steps{
                dir('cdk'){
                    sh '''
                    npm install;
                    npm run cdk-deploy;
                    '''
                }
            }
        }
    }
}
